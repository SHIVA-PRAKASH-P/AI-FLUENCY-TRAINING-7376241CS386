# Copy this file to .env and fill in the values for the provider you use.

# ollama | groq | huggingface
PROVIDER=ollama

# Only needed if PROVIDER=groq
GROQ_API_KEY=

# Only needed if PROVIDER=huggingface
HF_TOKEN=

# Optional: override the default model for your chosen provider
MODEL=
